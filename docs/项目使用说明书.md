# Flip Vault 项目使用说明书

## 1. 项目简介与功能范围

Flip Vault 是基于 Solana Devnet 与 MagicBlock Ephemeral Rollup（ER）构建的 10×10 翻箱子实时对战游戏，结合了链上资金托管与高频无 gas 交互体验。

### 核心功能
- **10×10 翻箱对抗**：每局包含 100 个红/绿双色箱子，链上记录每个箱子当前状态。
- **阵营分配与守护**：玩家报名后，由链上伪随机/种子算法平均分配至红方或绿方作为守护方。
- **保证金与翻箱计费**：
  - 玩家加入游戏需质押 **100 GAME** 保证金入池。
  - 玩家在报名时预充值点击额度（默认 100 GAME），每次翻箱扣除 1 GAME 计入本局奖池。
  - 游戏期间允许反复翻转任意箱子，所有翻箱花费均累积至奖池。
- **GAME Token 储备池兑换**：
  - 固定测试兑换率：`1 GAME = 0.001 Devnet SOL`。
  - 买入（Buy）：向金库转入 SOL，系统 Mint 相应 GAME Token 至玩家账户。
  - 赎回（Redeem）：销毁玩家 GAME Token，金库按比例退还 SOL（受金库当前 SOL 余额储备约束）。
- **游戏时间与结算规则**：
  - 报名期（Join Phase）：2 分钟（120 秒）。
  - 游戏期（Play Phase）：5 分钟（300 秒）。
  - 人数范围：每局 10～100 人。
  - 胜负判定：游戏期结束后，箱子数量占优的一方获胜，获胜阵营玩家按人头平分总奖池；平局时（50:50），所有玩家按各自实际入池代币比例原路退款；玩家未使用的翻箱额度在结算时全额返还。
- **MagicBlock Ephemeral Rollup (ER)**：
  - 支持将 Game 状态账户和 Player 账户一键委托（Delegate）至 MagicBlock ER 节点。
  - 在 ER 层实现亚秒级、免签名/免 gas 的翻箱交互。
  - 游戏结算后通过 `commit_and_undelegate` 将最终状态提交回 Solana Devnet 主链。

---

## 2. 架构与目录说明

```
flip-vault/
├── programs/
│   └── flip-vault/              # Pinocchio 链上合约 (Rust, no_std)
│       ├── Cargo.toml
│       └── src/
│           └── lib.rs           # 完整指令处理、PDA 校验、状态存储与 CPI
├── app/                         # Next.js 16 + React 19 + Tailwind 前端 dApp
│   ├── src/
│   │   ├── app/
│   │   │   ├── layout.tsx       # 全局布局与字体
│   │   │   ├── page.tsx         # 游戏主界面与交互面板
│   │   │   └── globals.css      # 赛博电竞风格暗黑主题样式
│   │   ├── components/
│   │   │   └── providers.tsx    # Solana Wallet Adapter 上下文
│   │   └── lib/
│   │       └── program.ts       # ABI 指令构建器、PDA 推导与 MagicBlock RPC
│   ├── scripts/
│   │   └── initialize.mjs       # 合约与测试 Game #0 初始化脚本
│   ├── package.json
│   ├── next.config.ts
│   └── tsconfig.json
├── deploy/
│   └── flip_vault-keypair.json  # 程序部署 Keypair（禁止外泄私钥）
├── docs/
│   ├── ABI.md                   # 详细二进制 ABI 与指令 Tag 说明
│   └── 项目使用说明书.md        # 本使用说明文档
├── tests/
│   └── abi.test.ts              # TypeScript ABI 与 PDA 自动化验证测试
├── Cargo.toml                   # Workspace 配置
├── package.json                 # 前端与测试 Workspace 脚本
└── pnpm-workspace.yaml
```

---

## 3. 环境与依赖

运行和构建本项目需要以下工具链：
- **Node.js**: `v20+` 或 `v22+`
- **pnpm**: `v9+` 或 `v10+`
- **Rust**: `1.85+` (stable)
- **Solana CLI**: `v2.1+` 或 `v2.2+`
- **cargo-build-sbf**: Solana SBF 合约编译工具
- **Vercel CLI**: `v40+`

---

## 4. 安装与本地启动

### 4.1 克隆仓库与安装依赖
```bash
git clone https://github.com/0xagentlabs/flip-vault.git
cd flip-vault
pnpm install
```

### 4.2 本地运行 dApp
```bash
cd app
pnpm dev
```
启动后在浏览器打开 `http://localhost:3000` 即可访问游戏。

---

## 5. Devnet 网络与钱包配置

- **网络**: Solana Devnet (`https://api.devnet.solana.com`)
- **MagicBlock Base RPC**: `https://rpc.magicblock.app/devnet`
- **MagicBlock Delegation Router**: `https://devnet-router.magicblock.app`
- **钱包配置**: 安装 Phantom、Solflare 或 Backpack 插件，切换网络为 **Devnet**，并确保钱包中有适量测试 SOL（可通过 `solana airdrop 1` 领取）。

---

## 6. Program ID、PDA 与关键地址

| 项目 | 地址 | 说明 |
|---|---|---|
| **Program ID** | `ADTfCpeekasxSNZNgSPgqfyRzxJ7BA4dtaBcoj8JQe8i` | 链上程序主地址 |
| **GAME Mint** | `Ay4P9UVG3X6TQ55JD5e5EWun8hAcUCc8SGn39EG79jdD` | 游戏代币 SPL Mint |
| **Config PDA** | `D1vHg92pWkDhZU5uQkmHGXe1tQaGSK5VXk6NT7L4f8Qs` | Seeds: `["config"]` |
| **Treasury PDA** | `BLPJpMjm8ehExJAPvM6KiUqUCvbx21CLTcBz6gJApdAA` | Seeds: `["treasury"]`，SOL 储备金库 |
| **Game #0 PDA** | `HvfCmHvWsRf1iQT4UVjMaxUqwqoGASh6nZjtNQpsJkJs` | Seeds: `["game", 0u64_le]` |
| **Player PDA** | `["player", game_pda, wallet_pubkey]` | 玩家局内状态与额度记账 |

---

## 7. 全部合约指令与操作指南

### 7.1 界面操作
1. **连接钱包**：点击右上角 `Connect Wallet` 按钮连接 Devnet 钱包。
2. **兑换代币**：在右侧控制面板点击 **“购买 200 GAME”**，签署交易后自动将 0.2 SOL 兑换为 200 GAME Token。
3. **加入战局**：在游戏编号输入框输入 `0`，点击 **“支付并加入”**，将支付 100 GAME 保证金 + 100 GAME 翻箱额度，系统随机分配红方或绿方。
4. **委托至 ER**：点击 **“委托我的玩家状态”** 与 **“房主委托游戏状态”**，将账户权限委托给 MagicBlock ER。
5. **开始游戏**：报名人数满足或到期后，点击 **“开始游戏”** 进入 5 分钟对抗期。
6. **翻箱互动**：在 10×10 网格中点击任意箱子，实时将箱子颜色翻转为己方颜色，每次扣减 1 GAME 额度进入奖池。
7. **结算与领奖**：游戏时间结束后点击 **“触发结算”**，随后点击 **“提交并解除委托”** 将状态回写主链，获胜方点击 **“领取奖励 / 退款”** 提取奖池收益。

### 7.2 脚本 / 命令行交互示例（Node.js / TypeScript）
```typescript
import { Connection, Keypair, PublicKey, Transaction, sendAndConfirmTransaction } from "@solana/web3.js";
import { buyIx, joinIx, flipIx, claimIx, pda } from "./app/src/lib/program";

const connection = new Connection("https://api.devnet.solana.com", "confirmed");
const payer = Keypair.fromSecretKey(/* 钱包私钥 Uint8Array */);

// 购买 100 GAME (花费 0.1 SOL)
const tx = new Transaction().add(buyIx(payer.publicKey, 100n));
const sig = await sendAndConfirmTransaction(connection, tx, [payer]);
console.log("Buy tx:", sig);

// 加入 0 号游戏 (100 GAME 保证金 + 50 GAME 点击额度)
const joinTx = new Transaction().add(joinIx(payer.publicKey, 0n, 50n));
await sendAndConfirmTransaction(connection, joinTx, [payer]);

// 翻转第 15 号箱子 (0-indexed, 0..99)
const flipTx = new Transaction().add(flipIx(payer.publicKey, 0n, 15));
await sendAndConfirmTransaction(connection, flipTx, [payer]);
```

---

## 8. 测试、构建与部署

### 8.1 单元测试与静态检查
```bash
# 运行 Rust 单元测试 (8/8 通过)
cargo test

# 运行 Rust Clippy 静态检查
cargo clippy -- -D warnings

# 格式化检查
cargo fmt --check
```

### 8.2 SBF 合约编译
```bash
NO_DNA=1 cargo build-sbf
```
构建产物位于 `target/deploy/flip_vault.so`。

### 8.3 TypeScript 集成测试与前端构建
```bash
# 验证 ABI 编码、Tag、指令账户与 PDA
pnpm run test:abi

# 前端代码检查与 Next.js 生产构建
pnpm test
```

---

## 9. 常见错误排查与错误码速查

| 错误码 | 标识 | 原因与解决方案 |
|---|---|---|
| `6000` | `E_INVALID_DATA` | 指令字节长度或格式不符合 ABI 要求，检查参数编码。 |
| `6001` | `E_INVALID_ACCOUNT` | 传入账户不匹配（例如 writable/signer 权限缺失或未传正确 PDA）。 |
| `6002` | `E_INVALID_OWNER` | 账户 Owner 不是本程序或对应的 Token Program。 |
| `6003` | `E_ALREADY_INITIALIZED` | Config 或 Game 已完成初始化，不能重复调用。 |
| `6004` | `E_BAD_STATUS` | 游戏状态不满足执行该指令的前提（如游戏未处于 Active 状态即执行翻箱）。 |
| `6005` | `E_JOIN_CLOSED` | 报名期已结束或已达 100 人上限，不可再加入。 |
| `6007` | `E_INSUFFICIENT_PLAYERS`| 报名人数未达到最低 10 人要求。 |
| `6010` | `E_OUT_OF_CREDITS` | 玩家预充值的点击额度已耗尽，需追加或等待结算。 |
| `6011` | `E_ALREADY_CLAIMED` | 该玩家已经领取过奖励或退款，不可重复领取。 |
| `6012` | `E_NOT_WINNER` | 玩家所在阵营不是获胜方，且非平局状态。 |
| `6014` | `E_RESERVE` | Treasury SOL 储备不足，暂时无法完全赎回。 |

---

## 10. 安全注意事项

1. **测试网络边界**：本项目仅部署并运行于 Solana Devnet，所有 GAME Token 及兑换的 SOL 均为测试资产，无现实经济价值。严禁部署到主网（mainnet-beta）。
2. **私钥隔离**：部署与管理 Keypair（`deploy/flip_vault-keypair.json`）仅在本地用于部署授权，绝不提交至代码仓库或在前端暴露。
3. **PDA 与签名校验**：所有敏感状态变更（游戏启动、结算、提款）均强制执行严格的 Signer 校验与 PDA 种子校验，防范伪造账户注入攻击。
4. **溢出安全**：所有代币计费、奖池累加与比例计算均采用 `checked_add`、`checked_mul` 等安全运算函数，发生溢出时立即终止事务。

---

## 11. 项目交付链接

- **GitHub 仓库**: [https://github.com/0xagentlabs/flip-vault](https://github.com/0xagentlabs/flip-vault)
- **Solana Devnet 浏览器**: [Solscan - ADTfCpeekasxSNZNgSPgqfyRzxJ7BA4dtaBcoj8JQe8i](https://solscan.io/account/ADTfCpeekasxSNZNgSPgqfyRzxJ7BA4dtaBcoj8JQe8i?cluster=devnet)
- **Vercel 生产应用**: [https://flip-vault.vercel.app](https://flip-vault.vercel.app)
- **ABI 文档**: `docs/ABI.md`
